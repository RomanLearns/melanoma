# Local tooling / config
.mcp.json

# Unzipped dataset (zip itself is tracked at the request of the team)
002/

# Trained model weights (too large for git)
SRC/capstone_results/best_model.keras

# Obsolete scratch notes (do not re-create)
cell_fixes.py
SRC/cell_fixes.py
